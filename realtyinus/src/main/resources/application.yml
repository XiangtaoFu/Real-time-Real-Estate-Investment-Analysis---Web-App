server:
  port: 8081

spring:
  application:
    name: realtyinus-service

logging:
  level:
    com.ireia.realty: DEBUG
  file:
    name: debug.log

realty:
  rapidapi:
    host: realty-in-us.p.rapidapi.com
    key: ${RAPIDAPI_KEY}
  aggregation:
    detailTimeoutSec: 20
    perPageEnrichLimit: 20
